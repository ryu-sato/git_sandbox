name: temp

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: echo ${{ github.ref }}
      - run: echo `git branch --remotes | tr -d ' ' || echo -n ""`
